<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" type="text/css" href="../navbar_style.css">
  <link rel="stylesheet" href="../UI.css">
  <style>
    .card-img-top {
      width: 100%;
      height: 15vw;
      object-fit: cover;
    }
  </style>
</head>

<body>
  <%- include ('navbar.ejs') %>
    <% if (article) { %>
      <h2 class="text-center" style="color: seagreen;
    font-family: 'Hill House', sans-serif;">
        <%= article[0].tag %>
      </h2>
      <% } else { %>
        <p>No article found.</p>
        <% } %>
          <div class="container mt-4">
            <div class="row">
              <% for (let i=0; i < article.length; i++) { %>
                <div class="col-12 col-md-6 col-lg-4 mb-4">
                  <div class="card h-100" id="cards">
                    <img src="<%= " ../" + article[i].image %>" class="card-img-top" alt="<%= article[i].title %>"
                      width="50%">
                      <div class="card-body">
                        <h5 class="card-title"><a href="/article/<%= article[i].art_id %>">
                            <%= article[i].title %>
                          </a></h5>
                        <p class="card-text">Date: <%= new Date(article[i].date).toLocaleDateString('en-US',
                            {month: 'long' , day: 'numeric' , year: 'numeric' }) %>
                        </p>
                      </div>
                  </div>
                </div>
                <% } %>
            </div>
          </div>

          <a href="/" class="text-center">Back</a>
</body>

</html>