<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" type="text/css" href="../navbar_style.css">
  <link rel="stylesheet" href="../UI.css">
  <title>StudART</title>
  <style> 
    form {
      max-width: 1000px;
      margin :0 auto;
      border-radius: 8px;
      padding: 30px
    }
  </style>
</head>

<body>
<%- include ('navbar.ejs') %>
<div class="container">
  <br>
  <div class="text-center"><h2>Update Article</h2></div>
  <br>
  <div class="text-center">
    <% if (article) { %> 
      <form action="/editarticle/<%= article.art_id %>" method="POST" enctype="multipart/form-data">
          <label for="title">title:</label><br>
          <textarea class="form-control" id="title" name="title" required><%= article.title %></textarea><br><br>

          <label for="content">content:</label><br>
          <textarea class="form-control" id ="content" name="content" required><%=article.content%> </textarea>

          <label for="tag">tag:</label><br>
          <textarea class="form-control" id="tag" name="tag" height="200px" required><%= article.tag %> </textarea><br><br>

          <label for="image">Current Image:</label><br>
          <input type="text" name="currentImage" value=<%= article.image %> >
          <img src = "/images/<%= "../" + article.image %>" width="20%"><br><br>
                     
          New Image:<input type="file" id="image" name="image" accept="image/*" value= <%= article.image %> ><br><br>

          <button type="submit">Update article</button>
      </form>
    <% } else { %>
        <p>No article found.</p>
    <% } %>
    <a href ="/">Back</a>
  </div>
</div>
</body>
</html>
